# برومبت شامل وكامل — بناء تطبيق Corvus VPN (تصميم Apple + معمارية VPN + باك اند Cloudflare)

انسخ البرومبت الكامل بالأسفل والصقه في Claude Code (بعد تثبيت Android Studio وربط حساب
Cloudflare على جهازك) ليبني لك المشروع كاملاً خطوة بخطوة، ملفاً بملف.

---

## البرومبت

```
ابنِ لي تطبيق Android احترافي متكامل باسم "Corvus VPN"، مبني بلغة Kotlin وJetpack
Compose (Material 3)، مع باك اند بسيط على Cloudflare. اتبع كل التفاصيل التالية بدقة.

==================================================
1) الهدف العام
==================================================
تطبيق VPN تجاري المظهر يخفي تماماً أنه يعمل بتقنية OpenVPN. المستخدم لا يرى أي واجهة
أو اسم متعلق بـ OpenVPN في أي مكان (لا في القوائم، لا في الإشعارات، لا في أي نص ظاهر).
لا يحتاج المستخدم لتثبيت أي تطبيق آخر إطلاقاً. كل شيء يحدث في الخلفية بصمت تام.

==================================================
2) هوية العلامة والتصميم (Apple Cupertino Aesthetic)
==================================================
- الاسم: Corvus VPN. الشعار: غراب مبسّط بخط واحد (Monoline)، هندسي بسيط بأسلوب
  SF Symbols، بلون Accent فقط.
- الخط: Inter أو Plus Jakarta Sans (بديل مفتوح المصدر مطابق لـ SF Pro)، أوزان
  Regular / Semibold / Bold فقط.
- نبرة النصوص: جمل قصيرة جداً بدون مصطلحات تقنية ("متصل" بدل "VPN Tunnel Active").

نظام الألوان (Design Tokens) في Color.kt:
  Light: Canvas #F2F2F7 | Card #FFFFFF | Text #1C1C1E | SecondaryText #8E8E93
         | Accent #007AFF | Success #34C759 | Danger #FF3B30
  Dark:  Canvas #000000 | Card #1C1C1E | Text #FFFFFF | SecondaryText #98989D
         | Accent #0A84FF | Success #30D158 | Danger #FF453A
  تبديل تلقائي عبر isSystemInDarkTheme()، بدون Dynamic Color (الهوية ثابتة).

الأشكال (Shape.kt) — Squircles لا زوايا دائرية عادية:
  SquircleShapeSmall = RoundedCornerShape(14.dp)   // عناصر صغيرة
  SquircleShape       = RoundedCornerShape(22.dp)  // بطاقات عادية
  SquircleShapeLarge  = RoundedCornerShape(28.dp)  // أزرار كبيرة / الكارد الرئيسي
  استخدم اختيارياً androidx.graphics.shapes (Shape Morph API في Compose 1.7+) لأشكال
  Squircle حقيقية على البطاقة الرئيسية.

الظلال والخامات (Materials):
  ظلال ناعمة جداً فقط (elevation 2.dp) بدون حدود؛ الفصل بين العناصر بالمسافة البيضاء.
  زر الاتصال الرئيسي بتأثير Glassmorphism خفيف (Modifier.blur + طبقة شفافة 85% فوق
  خلفية متدرجة متحركة ببطء) يحاكي .ultraThinMaterial في SwiftUI.

المكوّنات المطلوبة (كل واحد Composable مستقل قابل لإعادة الاستخدام):
  - CorvusButton: ارتفاع 56dp، Squircle، نص Semibold، ضغطة بتصغير خفيف (scale 0.97)
    مع spring animation.
  - CorvusCard: بطاقة بيضاء/غامقة، padding 20dp، ظل خفيف.
  - CorvusToggle: مطابق تماماً لسويتش iOS (أخضر عند التفعيل، انزلاق ناعم).
  - CorvusListRow: أيقونة + نص + Chevron يمين، بأسلوب Settings.app في iOS.
  - CorvusStatusRing: دائرة حالة الاتصال الرئيسية، نبض ناعم (scale 1 → 1.04 → 1 خلال 2
    ثانية)، رمادي = غير متصل، أخضر = متصل.

الحركة (Motion) — Spring لا Ease curves:
  val corvusSpring = spring<Float>(dampingRatio = Spring.DampingRatioMediumBouncy,
                                    stiffness = Spring.StiffnessLow)
  انتقال بين الشاشات عبر SharedTransitionLayout (Compose 1.7+). عند نجاح الاتصال:
  Haptic Feedback خفيف (HapticFeedbackType.Confirm) + تحول لوني تدريجي للدائرة خلال 400ms.

==================================================
3) الشاشات المطلوبة
==================================================
أ) الشاشة الرئيسية (Home):
   خلفية Canvas، عنوان "Corvus" صغير أعلى اليسار بخط خفيف الوزن. في المنتصف
   CorvusStatusRing بقطر 220dp وبداخله زر تشغيل/إيقاف دائري. تحتها اسم الدولة
   الحالية وعلمها ومدة الاتصال (نص ثانوي رمادي). أسفل الشاشة بطاقة أفقية فيها
   Ping / Download / Upload بثلاث أعمدة متساوية. بطاقة "تغيير السيرفر" قابلة للنقر
   بالكامل بعلم الدولة الحالية وسهم يمين. عند فشل سيرفر معيّن ينتقل التطبيق تلقائياً
   وبصمت لسيرفر آخر بنفس الدولة أو أقرب دولة، ويظهر فقط نص "جاري إيجاد أفضل اتصال..."
   دون أي رسالة خطأ فنية.

ب) شاشة اختيار السيرفر:
   شريط بحث بأسلوب iOS (خلفية رمادية فاتحة، أيقونة عدسة، Squircle صغير). قسم
   "الأفضل تلقائياً" في الأعلى ببطاقة مميزة بخلفية Accent خفيفة. قائمة الدول مجمّعة
   أبجدياً بعناوين قسم رمادية صغيرة (كجهات اتصال iOS)، كل صف: علم + اسم الدولة +
   مؤشر إشارة (3 أعمدة كإشارة واي فاي) بدل رقم ping خام.

ج) شاشة الإعدادات:
   قوائم CorvusListRow بأسلوب Settings.app: مجموعات مفصولة بعناوين رمادية صغيرة،
   كل مجموعة داخل بطاقة واحدة مقسّمة بخطوط رفيعة (1dp شفافية 10%). عناصر: Kill
   Switch (Toggle)، بدء تلقائي عند فتح الجهاز (Toggle)، إشعار دائم أثناء الاتصال
   (Toggle)، عن التطبيق.

==================================================
4) المعمارية التقنية لمحرك VPN
==================================================
- ادمج مكتبة ics-openvpn (https://github.com/schwabe/ics-openvpn) كـ Android
  Library Module داخل نفس المشروع (وليس كتطبيق منفصل)، واستخدم android.net.VpnService
  عبر واجهاتها البرمجية (OpenVPNService / ConfigParser) مباشرة بدون إظهار أي
  Activity/Fragment تابعة لها للمستخدم.
- أعد تسمية كل الموارد الظاهرة (اسم قناة الإشعارات، اسم الخدمة، أيقونة الإشعار)
  لتطابق هوية Corvus فقط، بدون أي ذكر لكلمة OpenVPN في أي String يظهر للمستخدم.
- Kill Switch حقيقي عبر VpnService: منع أي حزمة تخرج خارج النفق عند انقطاع الاتصال.
- خزّن ملفات .ovpn محلياً مشفّرة عبر EncryptedFile (Jetpack Security)، وليس كنص عادي.
- لا تسجّل (Logcat) أي بيانات اعتماد أو محتوى ملفات .ovpn في نسخة الإصدار (release)،
  فقط في وضع debug.

==================================================
5) الباك اند (Cloudflare Workers + R2/KV)
==================================================
ابنِ Cloudflare Worker (TypeScript) يقوم بـ:
- تخزين ملفات .ovpn في Cloudflare R2، منظمة كالتالي:
  /servers/{country_code}/{server_id}.ovpn
- GET /api/servers/manifest: يرجع JSON فيه قائمة كل السيرفرات المتاحة
  (country_code, country_name, server_id, city, load, status, updated_at, hash)
  لمقارنة سريعة بدون تحميل الملف كامل.
- GET /api/servers/{server_id}/config: يرجع محتوى ملف .ovpn، مع تحقق بسيط من
  API key في الهيدر (مفتاح قابل للتدوير من لوحة تحكم بسيطة).
- Cron Trigger كل ساعة: يتحقق من صحة كل سيرفر (health check عبر محاولة TCP connect
  للمنفذ) ويحدّث حقل status في المانيفست تلقائياً.
- Rate limiting بسيط على مستوى الـ Worker لمنع إساءة الاستخدام.

==================================================
6) منطق المزامنة داخل تطبيق أندرويد
==================================================
- عند كل فتح للتطبيق: اطلب /api/servers/manifest وقارن hash/version مع النسخة
  المخزّنة محلياً (Room Database أو DataStore). إن وُجد سيرفر جديد أو تحديث، نزّل
  ملفه فقط (لا تُعِد تنزيل كل شيء).
- شغّل WorkManager بمهمة دورية كل 24 ساعة (بقيد: يعمل فقط عند توفر إنترنت) تنفّذ
  نفس عملية المزامنة في الخلفية حتى لو كان التطبيق مغلقاً، بصمت تام بدون أي إشعار.
- كل الاتصال بالباك اند عبر HTTPS فقط، مع Certificate Pinning اختياري.

==================================================
7) نظام الإعلانات (Unity Ads Mediation) لتحقيق أقصى عائد ممكن
==================================================
ادمج Unity LevelPlay (Unity Ads Mediation SDK) بالمعرّفات التالية:
  Game ID: 6191257
  Banner Placement:      Banner_Android
  Interstitial Placement: Interstitial_Android
  Rewarded Placement:     Rewarded_Android

استراتيجية العرض (ضمن حدود تجربة مستخدم مقبولة، بدون إزعاج يدفع المستخدم لحذف
التطبيق — التوازن بين الربح والاحتفاظ بالمستخدم هو الأهم لتعظيم الربح على المدى
الطويل وليس فقط قصير المدى):

- Banner_Android:
  * شريط ثابت أسفل شاشة "اختيار السيرفر" فقط (ليس في الشاشة الرئيسية أثناء
    الاتصال، حتى لا يقطع تجربة نجاح الاتصال). استخدم Adaptive Banner Size.
  * حمّله مسبقاً (preload) عند دخول الشاشة، وأخفِه تماماً إن فشل التحميل
    (لا تترك مساحة فارغة قبيحة).

- Interstitial_Android:
  * يُعرض في نقطتين فقط، بحد أقصى مرة كل 3 دقائق (Frequency Capping فعلي في
    الكود عبر Timestamp محفوظ محلياً، وليس فقط اعتماداً على إعدادات Unity):
    1) بعد عملية "قطع الاتصال" (Disconnect) مباشرة — أبداً بعد "الاتصال" نفسه،
       لأن مقاطعة نجاح الاتصال تضر بمعدل الاحتفاظ بالمستخدمين.
    2) عند فتح شاشة اختيار السيرفر لأول مرة في الجلسة فقط (مرة واحدة لكل جلسة).
  * preload دائم لإعلان تالٍ فور عرض الحالي (Unity SDK يدعم preload تلقائياً
    عبر IUnityAdsLoadListener) لتقليل فرص الفشل وزيادة fill rate.

- Rewarded_Android (أعلى قيمة CPM عادة — استغلّه في "مزايا مقابل مشاهدة"):
  * "تسريع مؤقت": مشاهدة إعلان reward تمنح المستخدم أولوية اتصال بأفضل سيرفر
    (Premium-tier) لمدة ساعة، بدل الاتصال العشوائي العادي.
  * "فتح دول إضافية": بعض الدول (الأعلى طلباً) تكون خلف Rewarded بدل قفلها
    بالكامل، لزيادة نسبة المشاهدة الطوعية (Opt-in) بدل الإجبار — إعلانات
    اختيارية بحافز واضح تحقق eCPM أعلى بكثير من الإجبارية.
  * زر "احصل على اتصال أسرع" واضح وصادق (لا نصوص مضللة)، مع عدّاد واضح
    لمدة الميزة الممنوحة بعد المشاهدة.

- تطبيق Mediation Waterfall/Bidding الكامل لـ Unity LevelPlay (وليس شبكة Unity
  وحدها) لرفع fill rate والـ eCPM عبر منافسة الشبكات المتعددة تلقائياً — فعّل
  In-App Bidding إن كان متاحاً لحساب المطوّر.
- سجّل أحداث Analytics داخلية (بدون مكتبات تتبع خارجية إضافية) لقياس:
  Impression rate, eCPM تقريبي, معدل إكمال Rewarded, معدل حذف التطبيق بعد
  كل نوع إعلان — لضبط الترددات لاحقاً بناءً على بيانات حقيقية لا تخمين.
- لا تعرض أي إعلان أثناء استخدام الـ VPN فعلياً (Interstitial أو Rewarded) في
  المنتصف الفعلي للاتصال، فقط في نقاط انتقال طبيعية بين الشاشات كما هو موضح أعلاه.

==================================================
8) المخرجات المطلوبة منك (بالترتيب)
==================================================
1. هيكل مشروع Android Studio كامل (build.gradle.kts, settings.gradle.kts، كل الموديولات).
2. Theme.kt الكامل أولاً (Color.kt, Type.kt, Shape.kt).
3. المكوّنات (CorvusButton, CorvusCard, CorvusToggle, CorvusListRow, CorvusStatusRing).
4. الشاشات الثلاث (Home, اختيار السيرفر, الإعدادات).
5. كود دمج ics-openvpn ومنطق VpnService/Kill Switch.
6. كود Cloudflare Worker كامل جاهز للنشر عبر wrangler + تعليمات نشر خطوة بخطوة
   (wrangler deploy، إعداد R2 bucket، رفع الملفات الأولى).
7. منطق المزامنة (Manifest fetch + WorkManager) داخل التطبيق.
8. دمج Unity LevelPlay الكامل (Banner/Interstitial/Rewarded) بمنطق الترددات
   والـ preload الموضح أعلاه.
9. تعليمات فتح المشروع في Android Studio وبناء APK موقّع.

ابدأ بهيكل المجلدات الكامل، ثم Theme.kt، ثم انتقل ملفاً ملفاً حتى تنتهي كل الأقسام أعلاه.
```

---

## ملاحظة

هذا برومبت "توجيهي" لأداة برمجة تعمل في بيئة حقيقية متصلة بالإنترنت (Android Studio +
Cloudflare CLI) — غير متاحة في محادثتي الحالية لأن بيئتي هنا بلا اتصال إنترنت لتحميل
SDKs وبناء APK. انسخه إلى Claude Code على جهازك وسيبني لك المشروع كاملاً تفاعلياً.
# دليل استخدام لوجو Corvus VPN داخل التطبيق

لديك نسختان من اللوجو (رأس غراب بخط Monoline أسود):

| الملف | الوصف الفني |
|---|---|
| `LOGO_NO_BACKGROUND.png` | 1254×1254، RGBA، **خلفية شفافة فعلياً** (alpha=0) |
| `LOGO.png` | 1254×1254، RGB، **خلفية بيضاء صلبة** |

القاعدة العامة: **استخدم النسخة الشفافة (`LOGO_NO_BACKGROUND.png`) في كل مكان داخل
التطبيق تقريباً**، لأنها تتكيف تلقائياً مع أي خلفية (فاتحة/داكنة). النسخة ذات الخلفية
البيضاء تُستخدم فقط في الحالات القليلة التي تتطلب خلفية موحدة إجبارياً.

---

## 1. أيقونة التطبيق (App Icon / Launcher Icon)

**المصدر:** `LOGO_NO_BACKGROUND.png` — لكن يحتاج معالجة إضافية قبل الاستخدام مباشرة:

- أندرويد الحديث يتطلب **Adaptive Icon** (طبقتين: Foreground + Background منفصلتين)
  في `res/mipmap-anydpi-v26/ic_launcher.xml`.
- **Foreground layer:** الغراب نفسه (من النسخة الشفافة) لكن مصغّر داخل الـ Safe Zone
  (66% من المساحة الكلية، لأن أندرويد يقصّ الحواف حسب شكل الجهاز — دائرة/مربع مدور/قطرة).
- **Background layer:** لون Accent الخاص بالعلامة (`#007AFF` فاتح أو تدرّج بسيط)،
  وليس أبيض — لأن اللوجو أسود بالكامل وسيختفي على خلفية بيضاء في بعض أشكال القص.
- الأحجام المطلوبة: mdpi (48px) → xxxhdpi (192px)، بالإضافة لأيقونة Google Play
  عالية الدقة (512×512 PNG).
- **ملف `LOGO.png` (الخلفية البيضاء) لا يُستخدم هنا إطلاقاً** — لأن الأيقونة تحتاج
  التحكم الكامل بالخلفية بشكل منفصل عبر Adaptive Icon، لا خلفية مدمجة بالصورة.

## 2. شاشة البداية (Splash Screen)

**المصدر:** `LOGO_NO_BACKGROUND.png`

- استخدم `SplashScreen API` الرسمية من أندرويد 12+ (`androidx.core:core-splashscreen`).
- ضع اللوجو الشفاف في المنتصف فوق خلفية `Canvas` (بيضاء `#F2F2F7` أو سوداء `#000000`
  حسب الوضع الفاتح/الداكن)، بحجم حوالي 120dp.
- أضف أنيميشن بسيط: تكبير خفيف من 0.9 → 1.0 مع Fade-in خلال 300ms (spring animation
  بنفس فلسفة التصميم المتبعة في باقي التطبيق).

## 3. أيقونة الإشعارات (Notification Icon / Status Bar Icon)

**المصدر:** نسخة جديدة مُشتقة، **ليست أياً من الملفين مباشرة**.

- أندرويد يتطلب أيقونة إشعار **أحادية اللون وشفافة بالكامل** (Silhouette فقط،
  أبيض على شفاف) — النظام يلوّنها تلقائياً حسب الثيم.
- خذ شكل الغراب من `LOGO_NO_BACKGROUND.png` واملأه بلون أبيض 100% (بدلاً من الأسود)،
  بدون أي تدرجات، بحجم 24×24dp/48×48px. هذا الملف يوضع في `res/drawable/ic_notification.xml`
  (أفضل كـ Vector Drawable وليس PNG، لوضوح أعلى بكل الدقات).

## 4. الشاشة الرئيسية (Home Screen) — الترويسة العلوية

**المصدر:** `LOGO_NO_BACKGROUND.png`

- أيقونة صغيرة (28-32dp) بجانب اسم "Corvus" أعلى يسار الشاشة الرئيسية، بلون
  `LightText`/`DarkText` (وليس الأسود الثابت من الملف الأصلي) — لذا يُفضّل استيرادها
  كـ Vector Drawable وتلوينها ديناميكياً عبر `tint` بدل استخدام PNG أسود ثابت لا
  يتكيف مع الوضع الداكن.

## 5. متجر Google Play (Store Listing)

**المصدر:** كلا الملفين حسب الحاجة:

- **أيقونة المتجر (512×512):** نسخة معالجة من `LOGO_NO_BACKGROUND.png` مع خلفية
  Accent مضافة (نفس منطق Adaptive Icon أعلاه) — Google Play لا يقبل خلفية شفافة
  للأيقونة الرئيسية.
- **صور Feature Graphic / لقطات الشاشة الترويجية:** يمكن استخدام `LOGO.png`
  (بخلفيته البيضاء الجاهزة) مباشرة كعنصر تصميم في بانرات تسويقية بسيطة، أو كخلفية
  لصفحة "من نحن" داخل التطبيق نفسه.

## 6. شاشة "عن التطبيق" (About) داخل الإعدادات

**المصدر:** `LOGO_NO_BACKGROUND.png` — بحجم أكبر (80-100dp) في أعلى الشاشة، فوق
خلفية Canvas العادية للتطبيق (لا حاجة لخلفية بيضاء لأن الشفافية تتكفل بذلك).

## 7. أيقونة التطبيق المؤقتة أثناء التحميل / Placeholder

**المصدر:** `LOGO.png` (النسخة ذات الخلفية البيضاء) — مفيدة فقط كـ placeholder سريع
إن احتجت صورة "جاهزة بدون معالجة" لأي اختبار داخلي سريع قبل إنتاج Adaptive Icon
النهائي، لكنها لا تدخل في أي مخرج نهائي يراه المستخدم.

---

## ملخص سريع

| الموضع | الملف المصدر | يحتاج معالجة إضافية؟ |
|---|---|---|
| أيقونة التطبيق (Launcher) | NO_BACKGROUND | نعم — Adaptive Icon + خلفية Accent |
| شاشة البداية (Splash) | NO_BACKGROUND | لا |
| أيقونة الإشعارات | NO_BACKGROUND | نعم — تحويل لأبيض صامت (Silhouette) |
| ترويسة الشاشة الرئيسية | NO_BACKGROUND | يُفضّل Vector قابل للتلوين |
| أيقونة Google Play (512px) | NO_BACKGROUND | نعم — نفس معالجة Adaptive Icon |
| Feature Graphic / تسويق | LOGO.png (خلفية بيضاء) | لا |
| شاشة "عن التطبيق" | NO_BACKGROUND | لا |